name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  # -----------------------------------------------------------------------
  test-install-sh:
    name: install.sh (Linux)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run bash installer test harness
        run: bash scripts/test-installer.sh

  # -----------------------------------------------------------------------
  test-install-ps1:
    name: install.ps1 (Windows)
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run PowerShell installer test harness
        shell: pwsh
        run: pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/test-installer.ps1

  # -----------------------------------------------------------------------
  installer-drift-check:
    name: installer drift check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Rebuild installer zip in a tmpdir
        id: rebuild
        run: |
          set -euo pipefail
          TMP="$(mktemp -d)"
          # Copy the repo to a tmpdir so the build doesn't touch dist/ in place
          cp -r . "$TMP/hardgate"
          cd "$TMP/hardgate"
          python scripts/build-installer.py 1.1.0
          BUILT_HASH="$(sha256sum dist/hardgate-v1.1.0.zip | awk '{print $1}')"
          echo "built_hash=$BUILT_HASH" >> "$GITHUB_OUTPUT"

      - name: Hash the committed zip
        id: committed
        run: |
          set -euo pipefail
          COMMITTED_HASH="$(sha256sum dist/hardgate-v1.1.0.zip | awk '{print $1}')"
          echo "committed_hash=$COMMITTED_HASH" >> "$GITHUB_OUTPUT"

      - name: Compare
        run: |
          set -euo pipefail
          BUILT="${{ steps.rebuild.outputs.built_hash }}"
          COMMITTED="${{ steps.committed.outputs.committed_hash }}"
          echo "built:     $BUILT"
          echo "committed: $COMMITTED"
          if [ "$BUILT" != "$COMMITTED" ]; then
            echo "::error::installer drift: dist/hardgate-v1.1.0.zip does not match a fresh build from source."
            echo "::error::Rebuild with: python scripts/build-installer.py 1.1.0"
            exit 1
          fi
          echo "OK — committed installer matches source."

  # -----------------------------------------------------------------------
  pytest:
    name: "pytest / Python ${{ matrix.python-version }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install pytest
        run: pip install pytest

      - name: Run gate-hook test suite
        run: pytest tests/ -v --tb=short
